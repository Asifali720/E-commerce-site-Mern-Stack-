import Layout from "../components/Layout";
import CartSection from "../components/cart_section/CartSection";

const Cart = () => {
  return (
    <Layout>
      <CartSection />
    </Layout>
  );
};

export default Cart;
